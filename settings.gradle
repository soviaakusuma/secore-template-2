pluginManagement {
  repositories {
    mavenLocal()
    maven {
      url 'https://maven.inomial.com/'
    }
    gradlePluginPortal()
  }
}

// Defaults to dirname, but you can override here if you need
//rootProject.name = 'secore-template'
